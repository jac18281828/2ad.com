title: Playmmix — MMIX in a Browser Tab
slug: playmmix-browser-playground
tags: mmix, rust, wasm, debugger, checksmix
category: tech
date: 2026-09-05
status: draft
summary: A browser playground for MMIX, with a recursive primality test to put Step and Step Over to work.

[playmmix](https://playmmix.2ad.com) puts an MMIX machine in a browser tab. No
install, no server executing your program. I can send someone a link and get
straight to the interesting part: what does this instruction do?

MMIX is Knuth's 64-bit RISC architecture. I built
[checksmix]({filename}/posts/2025/10.checksmix-fast-mmix-emulator.md) to assemble
and run programs for it. playmmix puts an editor and a live debugger around
that same machine, using Rust, Yew and WebAssembly. Source goes on the left;
registers and memory are on the right, with output below the editor.

Editing the source re-assembles it and reloads the machine at the entry point.
Nothing executes until you ask it to.

![playmmix with the Hello World example halted and its output visible.]({static}/images/2026/playmmix-hello-world.jpg)

## Driving the machine

The controls are small enough to explain here:

- **Run** executes until the program halts or hits a breakpoint.
- **Step** executes one source-level step, following calls into the callee.
- **Step Over** runs a call until it returns, unless a breakpoint or halt stops it first.
- **Stop** interrupts execution where it is.
- **Reset** reloads the current source, clearing output and change highlights.

Click a line number to set a breakpoint on an instruction. The machine pane
highlights values that changed since the previous pause. Step once and see
what moved; run to a breakpoint and see what changed along the way.

## Is 97 prime?

Trial division is a small enough algorithm to hold in your head. Start with
divisor 2. If its square exceeds the number, we're done: prime. If it divides
the number evenly, we're also done: composite. Otherwise try the next divisor.

Make that last step a recursive call and we have something useful to debug.
Each call gets its own register window, and Step and Step Over have visibly
different jobs.

Paste this into playmmix:

```asm
        LOC     #100
Main    SETL    $1,97            % N (at least 2)
        SETL    $2,2            % first divisor
        PUSHJ   $0,IsPrime
        TRAP    0,Halt,0         % result in $0: 1 prime, 0 composite

IsPrime MULU    $3,$1,$1         % D * D
        CMPU    $3,$3,$0
        BP      $3,Prime
        DIVU    $3,$0,$1
        GET     $3,rR           % N mod D
        BZ      $3,Composite
        GET     $2,rJ           % preserve our return address
        SET     $4,$0           % next call's N
        ADDU    $5,$1,1         % next call's D
        PUSHJ   $3,IsPrime
        SET     $0,$3           % pass the result back
        PUT     rJ,$2
        POP     1,0
Prime   SETL    $0,1
        POP     1,0
Composite SETL  $0,0
        POP     1,0
```

This example is for small integers starting at 2. `SETL` loads a 16-bit
constant, which gives us plenty of numbers to try without making this a
general-purpose primality library.

Click **Run**. The program halts with `$0 = 1`. Change 97 to 91 and run again:
`$0 = 0`. Seven divides 91. The answer lives in the register; the halt message's
exit code is a separate value, left at zero in both cases.

## Following the call

Put 97 back and click **Reset**. Two clicks on **Step** load the arguments into
`$1` and `$2`. The next instruction is `PUSHJ $0,IsPrime`.

Click **Step** again. Now we're at `IsPrime`, call depth is 1, and the arguments
are in `$0` and `$1`: 97 and 2. The register numbers changed because the call
moved the register window. The caller stages arguments just above the register
named by `PUSHJ`; the callee sees them starting at `$0`.

Set a breakpoint on `PUSHJ $3,IsPrime`, the recursive call, and click **Run**.
We're about to try divisor 3. `$4` holds 97 and `$5` holds 3, ready to become
the next frame's `$0` and `$1`.

Use **Step** here and you enter that frame: call depth 2, divisor 3. Keep the
breakpoint and **Run** will stop before each deeper call. The divisor increases
while the number stays put.

Now **Reset**, then **Run** to the same breakpoint. Click its line number to
remove it and choose **Step Over**. All the deeper calls execute, then execution
pauses at `SET $0,$3`. We're back at call depth 1, with `$3 = 1`: the recursive
call found no factor. **Run** lets that result return to `Main` and halts.

Removing the breakpoint matters. Step Over still respects breakpoints inside
the call; leaving this one set would stop us in the next frame.

## Getting home

`PUSHJ` writes the return address to the special register `rJ`. A recursive call
will overwrite it, so `GET $2,rJ` saves it in a local register before we call
again. `PUSHJ $3,IsPrime` preserves the registers below `$3`, including that
saved address.

`POP 1,0` returns one value, placing the callee's `$0` in the caller's chosen
register: `$3` for the recursive call, `$0` for the call from `Main`. We copy
the result into our own `$0`, restore `rJ`, and return it another level.
The [MMIX instruction reference](https://mmix.cs.hm.edu/doc/instructions-en.html)
has the full register-stack rules.

For 97, the deepest call reaches divisor 10: 100 is greater than 97, so it
returns 1. For 91, divisor 7 leaves a zero remainder in `rR`, so it returns 0.
The same return path carries either answer home.

## Under the hood

checksmix owns the assembler and the machine: instruction execution, registers,
memory and TRAP handling. playmmix uses its public `MMixAssembler` and `MMix`
APIs. The editor, execution controls and machine display live in playmmix.

Re-assembly waits for a half-second pause in typing, so an unfinished line
doesn't flash an error on every keystroke. Run and Step flush any pending edit
before executing. A successful assembly starts a fresh machine; it doesn't
resume the old one against new source.

Long runs execute in bounded chunks, yielding to the browser between them.
That keeps Stop available, including while Step Over is working through a
call. The arithmetic runs locally in WebAssembly.

For `.mmo` object files or a shell, use
[checksmix](https://github.com/jac18281828/checksmix), also available on
[crates.io](https://crates.io/crates/checksmix). Its command-line debugger,
[mmixdb]({filename}/posts/2026/04.mmixdb-debugger.md), has its own worked example.

[Open playmmix](https://playmmix.2ad.com), paste the program, and try 49. Watch
which branch it takes when the divisor's square equals the number. The source
is on [GitHub](https://github.com/jac18281828/playmmix).
